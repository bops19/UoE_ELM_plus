# Optional enhanced PDF conversion dependencies for the app environment.
# Install after the base app requirements if you want:
# - Pix2Text full-document PDF -> Markdown conversion
# - pix2tex formula re-OCR for cropped equation regions
#
# Usage:
#   pip install -r requirements.txt
#   pip install -r requirements-pdf-enhanced.txt

pix2text==1.1.6
pix2tex==0.1.4
